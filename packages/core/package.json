{
  "name": "plumbery-core",
  "version": "1.0.0",
  "license": "AGPL-3.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "dev": "yarn build --watch",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json",
    "lint": "eslint --ext .ts ./src"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.29.0",
    "@typescript-eslint/parser": "^2.29.0",
    "eslint": "^6.8.0",
    "prettier": "^2.0.5",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "plumbery-connector-ethereum": "*",
    "plumbery-connector-thegraph": "*",
    "wonka": "^4.0.9"
  }
}
