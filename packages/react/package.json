{
  "name": "plumbery-react",
  "main": "dist/index.js",
  "version": "1.0.0",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "dev": "yarn build --watch",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "plumbery-core": "*"
  }
}
